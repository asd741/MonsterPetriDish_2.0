import React, { Component } from 'react';
import Layout from "../components/layout";

class Role extends Component {
    render() {
        return (
            <Layout>
                role
          </Layout>
        );
    }
}

export default Role;
